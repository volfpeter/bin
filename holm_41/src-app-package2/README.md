`holm` app with src layout and app package in `src`.

Based on the `holm` `actions-with-htmx` example.

Install and run with:
```bash
pip install -e .
my_app
```

Go to http://127.0.0.1:5000 and see the 404s. It works with:
```bash
ln -s src/my_app
```
